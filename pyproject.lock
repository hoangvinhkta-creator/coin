# Lockfile ghim dependency cho eth-dca-os (WP-A1/A1.6, đóng F-007, giảm thiểu RSK-006).
#
# SINH TỪ MÔI TRƯỜNG THẬT, không viết tay: đóng gói bắc cầu của các dependency khai trong
# `pyproject.toml` (numpy, pandas, pyarrow, requests + pytest cho dev), phiên bản đọc bằng
# `importlib.metadata` trên chính interpreter đã chạy backtest.
#
# `dependency_lock_hash` trong mỗi run record là sha256 của CHÍNH file này. Một dòng ghim
# sai phiên bản sẽ khiến record "chứng minh" một môi trường chưa từng tồn tại — đúng thất
# bại mà gói này sinh ra để chặn. `test_a1_08_lockfile_matches_installed_environment` đối
# chiếu từng dòng với môi trường đang chạy, nên lockfile viết tay không thể lọt qua.
#
# Python: 3.11.15
#
# Cập nhật: chạy lại đoạn sinh trong `test_a1_08_*` (hoặc `scripts/` nếu về sau có) trên
# môi trường mục tiêu, KHÔNG sửa tay từng dòng.

certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
iniconfig==2.3.0
numpy==2.4.6
packaging==24.0
pandas==3.0.5
pluggy==1.6.0
pyarrow==25.0.1
Pygments==2.21.0
pytest==9.1.1
python-dateutil==2.9.0.post0
requests==2.33.1
six==1.16.0
urllib3==2.6.3
